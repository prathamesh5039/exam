 int main() 
    {
            int m1, m2, sum;

            printf("Enter the two integers are: ");
            scanf("%d %d", &m1, &m2);

            // calculating sum
            sum = m1 + m2;

            printf("%d + %d = %d", m1, m2, sum);
            return 0;
    }


